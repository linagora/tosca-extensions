= Jabber Notifier

This extension allows to receive instant notification when a request is
submitted. It requires the gem 'xmpp4r', which is the main motivation
for its transformation into an extension.
